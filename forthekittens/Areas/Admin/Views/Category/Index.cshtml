@model List<Category>;
<!--&atif(TempData["success"]!=null)
{
    <h2>&atTempData["success"]</h2>
}
instead of here we'll put it in a partial view _Notification-->
<partial name="_Notification"/>
<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                Category List
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-area="Admin" asp-controller="Category" asp-action="Create" class="btn btn-secondary my-2 my-sm-0">
                <img src="~/paw.svg" alt="Paw Image" width="20" height="20" />Create new Category
            </a>
        </div>
    </div>
</div>
<table class="table table-bordered table-striped">  
    <thead>
    <tr>
            <th>Name</th>
            <th>Display Order</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td>@obj.Name</td>
                <td>@obj.DisplayOrder</td>
                <td>
                    <a class="btn btn-info" asp-area="Admin" asp-controller="Category" asp-action="Edit" asp-route-id="@obj.Id">Edit</a>
                </td>
                <td>
                    <a class="btn btn-warning" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@obj.Id">Delete</a>
                </td>
            </tr>
    
        }
    </tbody>
</table>